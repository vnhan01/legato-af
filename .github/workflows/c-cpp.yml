name: Testing
on:
  pull_request:
    branches:
    - master

jobs:
  build:
    runs-on: ubuntu-18.04
    steps:
    - name: configure
      run: |
        sudo apt-get install -yy repo
        echo "r
        repo init -u git://github.com/legatoproject/manifest
        repo sync
        ls -la
